
import React from 'react';
import { ContainerTable } from './styles';

export function TableStandard({ children }) {

    return (
        <ContainerTable>
            {children}
        </ContainerTable>
    );

}